<!-- <?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yidu.zj.dao.AfterUserDao">
	<select id="selectUser" resultType="AfterUser" parameterType="AfterUser">
		select * from afterUser where userName=#{userName} and userPassword=#{userPassword}
	</select>
	<insert id="insertAfterUser">
		INSERT INTO afteruser VALUE(#{userId},#{userName},#{userPassword},#{lastLoginTime},#{userSex},#{userPhone},#{userAddress})
	</insert>
</mapper> -->